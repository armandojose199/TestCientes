import { ResponseServicioClientes } from './response-servicio-clientes';

describe('ResponseServicioClientes', () => {
  it('should create an instance', () => {
    expect(new ResponseServicioClientes()).toBeTruthy();
  });
});
